# Base URL of the StayEase backend API.
# Local dev uses the Vite proxy, so "/api" works out of the box.
# For production builds, set the full URL, e.g. https://stayease-api.onrender.com/api
VITE_API_BASE_URL=/api
